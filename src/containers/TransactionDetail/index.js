import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const TransactionDetail = () => {
  return <Text>Transaction Detail</Text>;
};

export default TransactionDetail;
